@model ByteBite.Models.Order

@{
    ViewData["Title"] = "Edit Order";
}

<style>
    .form-container {
        background-color: #ffffff;
        padding: 2rem;
        border-radius: 8px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .control-label {
        font-weight: 600;
        margin-bottom: 0.5rem;
        display: block;
    }
</style>

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6 form-container">
            <h2 class="mb-4">Edit Order</h2>
            <hr />
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="Id" />

                <div class="form-group">
                    <label asp-for="OrderTime" class="control-label"></label>
                    <input asp-for="OrderTime" class="form-control" />
                    <span asp-validation-for="OrderTime" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="DiningTableId" class="control-label"></label>
                    <select asp-for="DiningTableId" class="form-select" asp-items="ViewBag.DiningTableId"></select>
                    <span asp-validation-for="DiningTableId" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="WaiterId" class="control-label"></label>
                    <select asp-for="WaiterId" class="form-select" asp-items="ViewBag.WaiterId"></select>
                    <span asp-validation-for="WaiterId" class="text-danger"></span>
                </div>

                <div class="form-group mt-4">
                    <button type="submit" class="btn btn-primary w-100">Save Changes</button>
                </div>
            </form>

            <div class="mt-3 text-center">
                <a asp-action="Index" class="text-decoration-none text-muted">Back to List</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}